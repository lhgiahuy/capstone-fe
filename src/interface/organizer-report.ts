export interface OverviewReport {
  noOfEvents: number;
  noOfRegistered: number;
  noOfUsersAttended: number;
  noOfUsersNotAttended: number;
}

export interface RegistrationDetail {
  noOfRegistered: number;
  month: number;
  year: number;
}
